spring:
  application:
    name: edunova-backend
  
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/edunova
    username: admin
    password: secret123
    
  sql:
    init:
      mode: never

logging:
  level:
    com.edunova: DEBUG
    org.springframework.r2dbc: DEBUG

server:
  port: 8080
